# My Quick Sort without recursion


## How to use

- Use `make` to build `run.exe`.
- Run `run.exe` to start the program.
- Follow the instruction of the program.

## Docs

- Just see the comments in the codes.